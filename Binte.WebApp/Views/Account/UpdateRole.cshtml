@{
    Layout = "~/Views/Shared/_LayoutPanel.cshtml";
}
@model RoleUpdateViewModel

<div class="row">
    <div class="col-12">
        <div class="card card-info">
            <div class="card-header">
                <h3 class="card-title">Rol Güncelle</h3>
            </div>
            <form class="form-horizontal" action="/Account/UpdateRole" method="post">
                <div class="card-body">
                    <div class="form-group row">
                        @Html.BSDisplayFor(p=>p.Name)
                        @Html.HiddenFor(p=>p.NormalizedName)
                        @Html.HiddenFor(p=>p.Id)

                        <div class="col-sm-10">
                            @Html.BSTextBox(p=>p.Name)
                            @Html.ValidationMessageFor(p=>p.Name)
                        </div>
                    </div>
                </div>
                <div class="card-footer">
                    <button type="submit" class="btn btn-info float-right">Kaydet</button>
                    <button href="/Account/Roles" class="btn btn-default float-right">Vazgeç</button>
                </div>
            </form>
        </div>
    </div>
</div>
